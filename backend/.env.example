# Application environment (development | staging | production)
APP_ENV=

# SQLite database (default for local development)
# Change to a full connection string for PostgreSQL in production
DATABASE_URL=

# GEMINI — required by the LLM reasoning layer (added in a later milestone)
GEMINI_API_KEY=
GEMINI_MODEL=